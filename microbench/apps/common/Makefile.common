
ARCH = $(shell uname -m)

ifeq ($(ARCH), x86_64)
  MSR = ../../../msr

  CPPFLAGS += -I$(MSR)
  LDFLAGS  += -L$(MSR) -lmsr

  ifdef PROFILING
    CPPFLAGS += -DPROFILING=$(PROFILING)
  endif
endif

LDFLAGS += -lm

CPPFLAGS += -D_GNU_SOURCE

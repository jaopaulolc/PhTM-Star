#!/usr/bin/make

target = librtm.a

DEFINES += -DRTM_ABORT_DEBUG

# DEFINES += -DRTM_CM_AUXLOCK

# DEFINES += -DRTM_CM_BACKOFF

CFLAGS += -O3 -Wall -I. -I../ -mrtm $(DEFINES)

all: $(target)

librtm.a: rtm.o
	$(AR) cr $@ $<

rtm.o: rtm.c
	$(CC) $(CFLAGS) -c -o $@ $<

clean:
	$(RM) *.a *.o

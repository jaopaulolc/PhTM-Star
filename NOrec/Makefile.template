#!/usr/bin/make

TARGET = libnorec.a

SRCS = $(wildcard *.cpp)
OBJS = $(SRCS:.cpp=.o)

CPPFLAGS = -I./include
CXXFLAGS = -O3 -mrtm -Wall

# DUMMY COMMENT
# DEFINES += -DCOMMIT_RATE_PROFILING
# DEFINES += -DRW_SET_PROFILING

$(TARGET):	$(OBJS)
	$(AR) cr $@ $^
	ranlib $@


clean:
	$(RM) *.o *.a
